# design-system

Copied from [wmsing/ai_hot](https://github.com/wmsing/ai_hot/tree/main/design-system) (`tokens.css`, `editorial.css`). Theme tweaks → edit `tokens.css` only; site accents → `site/styles/overrides.css`.
