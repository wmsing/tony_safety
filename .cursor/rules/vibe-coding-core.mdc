---
description: Vibe coding 核心工作流——计划、范围、审查、测试
alwaysApply: true
---

# Vibe Coding 核心约定

你是监督者协作方：先计划、再动手；改动要小、可审查。

## 每次新功能

1. 先出计划：要改哪些文件、函数签名、假设是什么；**先别写代码**
2. 等用户批准后再实现
3. 提示词必须有：Goal / Constraints / 不要动什么 / 如何验收

## 改动约束

- 只改用户点名的文件/目录；别顺手重构
- 公共 API 不要擅自重命名或改签名
- 新依赖、删文件、schema 变更：必须先说明并等批准

## 接受前 / 接受后

- 接受前总结：改了什么、删了什么、新依赖、潜在破坏点
- 有测试命令时，改完跑 typecheck + test + lint；失败先修再继续
- 任务交付闭环后，主动提醒用户：若开启下一个独立功能，建议开新 Chat 保持上下文纯净
- 若识别到用户提出与当前会话完全无关的新需求，主动询问是否开新对话再行动

## 不确定时

- 当用户指令模糊时，主动拆解成 Goal / Scope / Constraints 向用户确认，禁止直接脑补写代码
- 给 2 个选项让用户选
- 不猜测禁区意图；先问
