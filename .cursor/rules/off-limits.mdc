---
description: 禁区与敏感改动护栏（按项目改路径）
alwaysApply: true
---

# 禁区（Off-limits）

没明确批准前，不要改：

- `.env*` 以及写死密钥/凭证
- 任何 `*migration*` / schema 迁移文件
- 认证、支付、权限相关目录（本仓库若暂无，用户点名后同样遵守）

## 若必须碰禁区

1. 先出计划 + 回滚办法
2. 等用户明确同意
3. 改动尽量配测试

## Schema / 数据库

写代码前先给出：

1. up 变更
2. down / 回滚
3. 要跟着改的应用代码
4. 生产失败时怎么办
