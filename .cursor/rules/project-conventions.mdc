---
description: tony_safty 项目约定 — 命令、结构、禁区
alwaysApply: true
---

# Project conventions (tony_safty)

## Run / quality (fill when stack is chosen)
- **install**: TBD
- **dev**: TBD
- **typecheck**: TBD
- **test**: TBD
- **lint**: TBD

## Layout (update as code lands)
- `.cursor/` — Agent 安全与 hooks（勿删安全项）
- `content/posts/` — 博文 Markdown（静态站生成器待定）
- `content/drafts/`、`data/raw/` — 预留：资讯抓取草稿与原始数据（未建目录时可后补）

## Hard off-limits (never edit without explicit user OK)
- `.env*`（除 `.env.example`）
- 密钥、凭证、支付、生产认证相关文件
- `*migration*` / schema 迁移（须先给 up/down 方案）
- `.cursor/cli.json`、hooks 的 fail-closed 行为（除非在做安全基线维护）

## Workflow
- 新功能：先列方案与文件清单，用户批准后再写代码。
- 版本管理：Git + GitHub；密钥仅 `.env`（仓库用 `.env.example` 列变量名）。
- 参照黄金样板（确立后写在仓库 README 或此处）：TBD
